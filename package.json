{
  "name": "grill",
  "version": "1.8.2",
  "description": "Spaced repetition and active recall for your own notes: AI writes flashcard-style questions, grades your answers, and tracks what you keep getting wrong. For Obsidian.",
  "main": "main.js",
  "scripts": {
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "esbuild": "0.24.0",
    "obsidian": "^1.7.2",
    "typescript": "^5.7.0"
  }
}
